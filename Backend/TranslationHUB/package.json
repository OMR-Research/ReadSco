{
  "name": "translationhub",
  "version": "1.0.0",
  "description": "Main hub to communicate with the Python Services",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "launch": "nodemon build/main.js",
    "deploy": "npm run build && npm run launch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Antonio Ríos",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@types/express": "^4.17.1",
    "@types/jsonwebtoken": "^8.3.3",
    "@types/multer": "^1.3.9",
    "@types/socket.io": "^2.1.2",
    "@types/socket.io-client": "^1.4.32",
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "multer": "^1.4.2",
    "socket.io": "^2.5.0",
    "socket.io-client": "^2.2.0"
  }
}
