import { ComponentLoadingDirective } from './component-loading.directive';

describe('ComponentLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
